# Test-specific properties
# This overrides the main ddl-auto=validate setting only during tests.
# It makes Hibernate create the schema, allowing us to see the expected DDL in the logs.
spring.jpa.hibernate.ddl-auto=create-drop
